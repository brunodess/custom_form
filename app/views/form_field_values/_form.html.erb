<%= form_for(@form_field_value) do |f| %>
  <% if @form_field_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@form_field_value.errors.count, "error") %> prohibited this form_field_value from being saved:</h2>

      <ul>
      <% @form_field_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :form_field_id %><br>
    <%= f.number_field :form_field_id %>
  </div>
  <div class="field">
    <%= f.label :is_default %><br>
    <%= f.check_box :is_default %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
